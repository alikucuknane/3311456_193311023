import 'package:flutter/material.dart';

class AdminAddCompany extends StatelessWidget {
  const AdminAddCompany({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Container(),
    );
  }
}
