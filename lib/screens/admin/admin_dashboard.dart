import 'package:flutter/material.dart';

class AdminDasboardScreen extends StatelessWidget {
  const AdminDasboardScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
